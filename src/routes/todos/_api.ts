let todos: Todo[] = [];

export async function api( {request} ) {
    
    switch(request.method.toUpperCase() ) {
        case "GET":
            break;
        default:
            break;
    }

        
}